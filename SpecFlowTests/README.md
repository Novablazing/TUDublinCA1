SpecFlow BDD tests for BPCalculator. These tests reference the `BPCalculator` project in the solution.

To run the tests:
1. Ensure you have .NET 9 SDK installed.
2. From the solution directory run: `dotnet test SpecFlowTests/SpecFlowTests.csproj`